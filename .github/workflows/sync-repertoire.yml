name: Sync student repertoire

on:
  schedule:
    - cron: '17 10 * * 1'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    if: ${{ secrets.SONGS_TAUGHT_CSV_URL != '' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/sync-repertoire.mjs
        env:
          SONGS_TAUGHT_CSV_URL: ${{ secrets.SONGS_TAUGHT_CSV_URL }}
      - run: |
          git diff --quiet -- public/repertoire.json || {
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add public/repertoire.json
            git commit -m 'Sync student repertoire'
            git push
          }
